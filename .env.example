# Pilates Studio Management - Environment Variables

# API Configuration
EXPO_PUBLIC_API_URL=http://localhost:3000/api/trpc
EXPO_PUBLIC_BACKEND_URL=http://localhost:3000

# Mock Data Mode
# Set to 'true' to use mock data instead of real API calls
EXPO_PUBLIC_ENABLE_MOCK_DATA=true

# Debug Mode
# Set to 'true' to enable debug logging
EXPO_PUBLIC_ENABLE_DEBUG_MODE=true

# Expo Go Debug
# Set to 'true' when testing with Expo Go
EXPO_GO_DEBUG=true

# Network Configuration
# For Expo Go testing, use your local IP address
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api/trpc
# Find your IP:
#   - macOS: ifconfig | grep "inet " | grep -v 127.0.0.1
#   - Windows: ipconfig
#   - Linux: ip addr show | grep "inet " | grep -v 127.0.0.1

# Tunnel Configuration (for remote testing)
# If using ngrok: EXPO_PUBLIC_API_URL=https://your-ngrok-url.ngrok.io/api/trpc
# If using Expo tunnel: EXPO_PUBLIC_API_URL will be auto-configured
